#include <vector>
#include "FileIO.h";
using namespace std; 

vector<vector<char>> loadFile() {
	char* fileName;
	vector<vector<char>> vect;
	return vect;
	
}

int saveFile(vector<vector<char>>* maze) {

	return 0;
}